"user strict";
// Крок 0 Виводимо позначення
// - місяць - month
// Крок 1 Введення величин
let month = 0;
// Крок 2 Розв'язання задачі
for (let month = 3; month <= 8; month++) {
  // Крок 3  Виводимо результат
  document.write(`<div class="button">Місяць ${month}</div>`);
}
